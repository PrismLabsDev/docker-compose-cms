version: '3.8'

services:

  maintenance:
    build:
      context: ./docker
      dockerfile: maintenance.dockerfile
    restart: always
    volumes:
      - ./docker/volumes/maintenance/certbot:/var/certbot/
      - ./docker/volumes/maintenance/letsencrypt/:/etc/letsencrypt/
    depends_on:
      - nginx

  nginx:
    build:
      context: ./docker
      dockerfile: nginx.dockerfile
    restart: always
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./docker/config/nginx/conf.d/:/etc/nginx/conf.d/
      - ./docker/config/nginx/sites/default:/var/www/default
      - ./docker/volumes/postfixadmin/html/:/var/www/postfixadmin
      - ./docker/volumes/webmail/html/:/var/www/webmail
      - ./docker/volumes/maintenance/certbot:/var/certbot/
      - ./docker/volumes/maintenance/letsencrypt/:/etc/nginx/ssl/
    depends_on:
      - strapi
      - mailserver

  strapi:
    build:
      context: ./docker
      dockerfile: strapi.dockerfile
    restart: always
    ports:
      - 1337:1337
    environment:
      DATABASE_CLIENT: mongo
      DATABASE_HOST: mongo
      DATABASE_PORT: 27017
      DATABASE_NAME: strapi
      DATABASE_USERNAME: strapi
      DATABASE_PASSWORD: secret
    volumes:
      - ./docker/volumes/strapi/app:/srv/app
    depends_on:
      - mongo

  mongo:
    build:
      context: ./docker
      dockerfile: mongo.dockerfile
    restart: always
    ports:
      - '27017:27017'
    environment:
      MONGO_INITDB_ROOT_USERNAME: strapi
      MONGO_INITDB_ROOT_PASSWORD: secret
    volumes:
      - ./docker/volumes/mongo/data:/data/db

  mailserver:
    build:
      context: ./docker
      dockerfile: mailserver.dockerfile
    restart: always
    hostname: mail
    domainname: example.com
    env_file: ./docker/config/mailserver/mailserver.env
    ports:
      - "25:25"    # SMTP  (explicit TLS => STARTTLS)
      - "143:143"  # IMAP4 (explicit TLS => STARTTLS)
      - "465:465"  # ESMTP (implicit TLS)
      - "587:587"  # ESMTP (explicit TLS => STARTTLS)
      - "993:993"  # IMAP4 (implicit TLS)
    volumes:
      - ./docker/volumes/mailserver/mail-data/:/var/mail/
      - ./docker/volumes/mailserver/mail-state/:/var/mail-state/
      - ./docker/volumes/mailserver/mail-logs/:/var/log/mail/
      - ./docker/volumes/mailserver/config/:/tmp/docker-mailserver/
      - ./docker/volumes/maintenance/letsencrypt/:/etc/letsencrypt/
      - /etc/localtime:/etc/localtime
    stop_grace_period: 1m
    cap_add:
      - NET_ADMIN
    healthcheck:
      test: "ss --listening --tcp | grep -P 'LISTEN.+:smtp' || exit 1"
      timeout: 3s
      retries: 0

  # webmail:
  #   build:
  #     context: ./docker
  #     dockerfile: webmail.dockerfile
  #   restart: always
  #   ports:
  #     - 9000:9000
  #   volumes:
  #     - ./docker/volumes/webmail/html/:/var/www/html
  #   environment:
  #     - ROUNDCUBEMAIL_DEFAULT_HOST=tls://mail.experimental.cyou
  #     - ROUNDCUBEMAIL_DEFAULT_PORT=143
  #     - ROUNDCUBEMAIL_SMTP_SERVER=tls://mail.experimental.cyou
  #     - ROUNDCUBEMAIL_SMTP_PORT=587
  #     - ROUNDCUBEMAIL_UPLOAD_MAX_FILESIZE=5M

  # postfixadmin:
  #   build:
  #     context: ./docker
  #     dockerfile: postfixadmin.dockerfile
  #   ports:
  #     - 9000:9000
  #   volumes:
  #    - ./docker/volumes/postfixadmin/html/:/var/www/html
  #   restart: always
  #   environment:
  #     POSTFIXADMIN_DB_TYPE: mysqli
  #     POSTFIXADMIN_DB_HOST: db
  #     POSTFIXADMIN_DB_USER: postfixadmin
  #     POSTFIXADMIN_DB_NAME: postfixadmin
  #     POSTFIXADMIN_DB_PASSWORD: example

  # mysql:
  #   build:
  #     context: ./docker
  #     dockerfile: mysql.dockerfile
  #   restart: always
  #   environment:
  #     MYSQL_RANDOM_ROOT_PASSWORD: 1
  #     MYSQL_DATABASE: postfixadmin
  #     MYSQL_USER: postfixadmin
  #     MYSQL_PASSWORD: example
